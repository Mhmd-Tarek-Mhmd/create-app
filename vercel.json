{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" }
  ],
  "buildCommand": "yarn build",
  "outputDirectory": "dist",
  "env": {
    "NODE_VERSION": "22"
  }
}
